fast-syntx-highlighting problems:
	breaks when calling a completion widget
	breaks on fzf-tab
	unsets -match functions

man trap
man 7 signal
man ps
# to figure out which CHAR sends signals
man stty

find why comment lines are being removed from history

git diff show tabs and escape characters
application mode vs raw mode
chnage github tabsize using modelines

zshall compcontext

learn lua
read man zshcontrib

nvim g; g- g+
nvim show search highlighting only when on word
marks on cmdwin
nvim i_CTRL-R_" to paste charwise
nvim :pu[!] to paste linewese
git add -p # include untracked files
increase highlight priority TextYankHighlight
The -- more -- prompt
nvim-treesitter vim not working on cmdline window
nvim-treesitter highlight doesnt work when zl
nvim add changes to qflist from git
nvim :f := :bm

nvim :h ex-commands
nvim :h redo-register
nvim ga # get ascii
nvim digraphs
nvim :h index
nvim [_CTRL-i
nvim [_I
nvim [_i
nvim ]_CTRL-i
nvim ]_I
nvim ]_i

zshall 'PARAMETERS SET BY THE SHELL'
nvim universal ctags
nvim :h ins-completion
nvim :h tag-stack
man zshoptions SH_FILE_EXPANSION
# interesting
man zshzle recursive-edit
man zshall MATCH

chsh - <TAB> redraws prompt

nvim auto :set ft=zsh for files in fpath

syntax highlighting on << EOF broken
syntax highlighting on regex on grep|sed|awk

androfecth CPU find

fixnvim SIGHUP

add nvim diff swap file

 man -a # not working

run-help and which-command clear autosuggestion

no syntax highlighting when creating func .ex mman() { man -a $@ }

 fzf-tab glob operators, glob flags and glob qualifiers


nvim vi" keys doesnt pair correctly

_path_files completion
cd ~/./d/z
