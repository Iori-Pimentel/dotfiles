git commit --fixup :/second
find all ways to refer to commits or refs
git pathspec
git refs
git-config-info breaks on :
add more git aliases

zmodload module
zle redisplay or reset-prompt

zsh-autosuggestion problems:
	race condition
	accept-line on syntax error breaks it

fast-syntx-highlighting problems:
	breaks when calling a completion widget
	breaks on fzf-tab
	dont like chroma on commands

git diff show tabs and escape characters
chnage github tabsize using modelines

nvim-treesitter vim not working on cmdline window
nvim add changes to qflist from git

nvim universal ctags
nvim :h ins-completion

androfecth CPU find

fix nvim SIGHUP

man -a # not working
